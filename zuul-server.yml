server:
  port: 8091 #the default port used for zuul service
  
zuul:
  ignoredServices: '*'
  routes:
    path: /api/** #path that will be intercepted
    url: http://localhost:8090  #host y puerto donde se levanto la API
